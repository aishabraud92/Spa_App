'use strict'
var appointmentEmitter = require('appointments').appointmentEmitter
var util = require('util')

var trackerId = 0
var TrackerBase = module.exports = function (name) {
  appointmentEmitter.call(this)
  this.id = ++trackerId
  this.name = name
}
util.inherits(TrackerBase, appointmentEmitter)
